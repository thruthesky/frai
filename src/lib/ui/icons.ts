/**
 * 아이콘 path 데이터.
 *
 * 왜 패키지를 쓰지 않는가: 의존성을 하나 늘릴 때마다 공급망 공격 표면이 늘어난다
 * (CLAUDE.md "의존성 최소"). 이 앱이 쓰는 아이콘은 아홉 개뿐이고 전부 24x24 격자의
 * 단순한 선 도형이라, 패키지를 들이는 것보다 path 문자열을 직접 두는 편이 싸다.
 * 형태는 Lucide(MIT) 의 아이콘 규격 — 24x24 viewBox, 선 굵기 1.75, 둥근 끝 — 을 따른다.
 *
 * 마크업이 아니라 **데이터**로 두는 것이 중요하다. 이러면 Icon.svelte 가 `{@html}` 없이
 * `<path d={...}>` 로 바인딩할 수 있다. LLM 응답을 다루는 앱에서 `{@html}` 사용처는
 * sanitize 를 거친 한 곳으로만 모아야 하므로(CLAUDE.md 보안 원칙 3), 아이콘 때문에
 * 그 규칙을 흐리지 않는다.
 */
export const ICONS = {
  plus: 'M5 12h14M12 5v14',
  x: 'M18 6 6 18M6 6l12 12',
  /** 스트리밍 중단 — 정지 사각형 */
  stop: 'M6 6h12v12H6z',
  chevronDown: 'm6 9 6 6 6-6',
  send: 'm22 2-7 20-4-9-9-4Z',
  check: 'M20 6 9 17l-5-5',
  alert: 'M12 9v4M12 17h.01M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0Z',
  /** 대화가 기기를 벗어나는 경로(릴레이) */
  globe: 'M12 2a10 10 0 1 0 0 20 10 10 0 0 0 0-20M2 12h20M12 2a15 15 0 0 1 0 20 15 15 0 0 1 0-20',
  /** 로컬에서 실행되는 경로(CLI·Ollama) */
  cpu: 'M6 6h12v12H6zM9 2v4M15 2v4M9 18v4M15 18v4M2 9h4M2 15h4M18 9h4M18 15h4',
} as const

/** `as const` 덕분에 리터럴 유니온이 된다 — 오타를 `npm run check` 가 잡는다. */
export type IconName = keyof typeof ICONS
